# Indica que este diretório é a raiz para as configurações do Lombok
config.stopBubbling = true

# Adiciona a anotação @Generated aos métodos gerados pelo Lombok
lombok.addLombokGeneratedAnnotation = true
